export const fetchNotifications = () => {
  return async (dispatch) => {
    // Implémentez la logique pour récupérer les notifications
  };
};

export const markNotificationAsRead = (notificationId) => {
  return async (dispatch) => {
    // Implémentez la logique pour marquer une notification comme lue
  };
}; 