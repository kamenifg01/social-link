import { getPosts, createPost, updatePost, deletePost } from '../postService';

jest.mock('axios');

test('fetches posts successfully', async () => {
  // Implémentez le test pour récupérer les posts
});

test('creates a post successfully', async () => {
  // Implémentez le test pour créer un post
});

test('updates a post successfully', async () => {
  // Implémentez le test pour mettre à jour un post
});

test('deletes a post successfully', async () => {
  // Implémentez le test pour supprimer un post
}); 