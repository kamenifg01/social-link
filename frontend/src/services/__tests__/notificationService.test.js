import { getNotifications, markAsRead } from '../notificationService';

jest.mock('axios');

test('fetches notifications successfully', async () => {
  // Implémentez le test pour récupérer les notifications
});

test('marks notification as read successfully', async () => {
  // Implémentez le test pour marquer une notification comme lue
}); 