import { getUserProfile, updateUserProfile } from '../profileService';

jest.mock('axios');

test('fetches user profile successfully', async () => {
  // Implémentez le test pour récupérer le profil de l'utilisateur
});

test('updates user profile successfully', async () => {
  // Implémentez le test pour mettre à jour le profil de l'utilisateur
}); 